Experimental Tables, Algorithms, and Equations Required to Strengthen the Manuscript1. Experimental Tables RequiredIDExact placementConcise captionDescription for drafting from resultsT1Insert in Section 5.1, immediately after the sentence that states that benchmark intrusion detection data were used.Datasets and attack-class characteristics used in the experiments.Include dataset name, source, version, number of records, number of features, benign records, attack records, attack classes, imbalance ratio, training set size, validation set size, test set size, and reason for selecting the dataset. This table is required because the current manuscript does not identify the datasets used.T2Insert in Section 5.1, after the dataset table.Preprocessing and feature-engineering protocol.List cleaning steps, missing-value handling, categorical encoding, feature scaling, feature selection if any, temporal windowing for sequence models, class balancing, leakage prevention, and train-test split policy. This table will make the reported detection results reproducible.T3Insert in Section 5.1, after the preprocessing table.Experimental testbed and implementation configuration.Include processor, RAM, GPU if used, operating system, Python or framework versions, SDN controller if used, virtual machines, honeypot platform, traffic generator, attack tools, number of independent runs, random seeds, and runtime environment.T4Insert in Section 4.3 or Section 5.1, before reporting detection results.Detection-model configuration and hyperparameters.For each detector, report model name, input type, feature dimension, architecture, optimizer, learning rate, batch size, epochs, early stopping, class weighting, and decision threshold. Include both the hybrid model and all baseline detection models.T5Insert in Section 4.4 or Section 5.1, before the deception results.Adaptive honeypot and deception-layer configuration.Report decoy services, redirection criteria, engagement threshold, logging variables, reconfiguration triggers, interaction timeout, fingerprinting safeguards, and intelligence items extracted from attacker interaction.T6Insert in Section 4.5 or Section 5.4, before the response evaluation.Reinforcement-learning or multi-agent reinforcement-learning configuration.Include state variables, action set, reward terms, policy model, training episodes, discount factor, exploration strategy, update rule, safety constraints, and response actions such as monitor, redirect, rate-limit, isolate, migrate, or block.T7Replace the current Table 2 in Section 5 with this table.Main performance results of the proposed framework.Report mean and standard deviation across repeated runs for accuracy, precision, recall, F1-score, ROC-AUC, false positive rate, false negative rate, containment time, mitigation time, deception engagement rate, and resource overhead. Include 95 percent confidence intervals rather than only target and status values.T8Insert in Section 5.2, after the main performance table.Per-attack-class detection results.Report precision, recall, F1-score, support, false positive rate, and false negative rate for each attack class. This is necessary because high overall accuracy may hide poor performance on rare or severe attacks.T9Insert in Section 5.3, before the deception engagement figure.Deception and threat-intelligence collection results.Include number of redirected sessions, successful engagement rate, average dwell time, commands captured, payloads captured, unique attacker behaviors, decoy escape attempts, and intelligence items reused by the learning layer.T10Insert in Section 5.4, after the reinforcement-learning configuration table.Autonomous response performance by attack scenario.For each attack type, report selected action, containment success rate, initial containment time, full mitigation time, collateral blocking of legitimate users, response cost, and number of policy changes after learning.T11Insert in the new Ablation Study subsection after Section 5.4.Ablation study of the proposed framework components.Compare the full framework with detection-only, detection plus static honeypot, detection plus adaptive honeypot, detection plus RL response, single-agent control, no learning feedback, and no multi-agent communication. Report changes in detection, response, deception, and overhead metrics.T12Insert in the new Robustness Evaluation subsection after the ablation table.Robustness evaluation under realistic threat and system variations.Report performance under unseen attacks, concept drift, noisy traffic, adversarial evasion, delayed agent communication, agent failure, and honeypot fingerprinting attempts. Use mean, standard deviation, and percentage degradation from the clean test setting.T13Insert in Section 5.5, after the scalability figure.Scalability and resource-overhead results.Report volume of traffic, number of agents, number of monitored hosts, throughput, decision latency, communication overhead, CPU use, memory use, and detection delay. This table will support the claim that the distributed architecture is scalable.T14Replace the current Table 3 in Section 5.6 with a fair comparison table.Comparison with baseline methods under the same dataset and testbed.Compare the proposed system with baseline systems trained and evaluated under the same conditions. Include dataset, model, detection metrics, response metrics, deception metrics, overhead, and whether the method supports continuous learning. Avoid mixing values from unrelated papers unless clearly marked as a literature-only comparison.T15Insert in Section 5.6, after the fair comparison table.Statistical significance analysis against baseline methods.Report paired tests across repeated runs, such as paired t-test or Wilcoxon signed-rank test depending on normality, with p-values, effect sizes, and adjusted significance level. This is important for Q1-level credibility.T16Insert as an appendix or final subsection of Section 5 titled Reproducibility Summary.Reproducibility checklist for experimental evaluation.List dataset availability, code availability, model parameters, random seeds, hardware, software versions, number of runs, evaluation scripts, and limitations. This table will help reviewers verify whether the study can be replicated.2. Algorithms to Draft and IncludeIDExact placementAlgorithm titleWhat the algorithm should containA1Insert at the end of Section 4.8 Operational Workflow.Overall HoneyBee-inspired Detect--Mislead--Neutralize--Learn workflow.Provide pseudocode that starts with incoming network events, assigns detection agents, computes threat score, decides normal monitoring or honeypot redirection, selects response action, logs attacker interaction, updates learning memory, and updates detection or response policies.A2Insert after Section 4.7 Multi-Agent Coordination.Multi-agent coordination and message-sharing procedure.Define how detection agents, deception agents, response agents, and learning agents exchange alerts, confidence scores, intelligence summaries, and response decisions. Include conflict resolution and fallback behavior when an agent fails or communication is delayed.A3Insert after Section 4.4 Deception Layer.Adaptive honeypot selection and reconfiguration algorithm.Specify how the system selects a decoy service, redirects suspicious traffic, records attacker behavior, detects honeypot fingerprinting, reconfigures the decoy, and sends intelligence to the learning layer.A4Insert after Section 4.5 Response Layer.Reinforcement-learning-based autonomous response policy.Provide pseudocode for state observation, action selection, safety validation, execution of response action, reward computation, and policy update. If a multi-agent method is used, specify whether agents learn independently or through a shared critic or shared memory.A5Insert after Section 4.6 Learning Layer.Continuous learning and model update procedure.Describe how new labeled or weakly labeled incidents are validated, stored, used to update the detection model, and used to refine the response policy. Include safeguards against poisoning from deceptive or adversarial data.3. Equations to Draft and IncludeIDExact placementEquation nameWhat the equation should defineE1Insert in Section 4.3 Detection Layer, before describing hybrid detection decisions.Hybrid threat score fusion.Define a fused threat score, for example a weighted combination of machine-learning probability, deep-learning anomaly score, rule-based alert score, and deception-trigger score. This equation will make the detection decision transparent.E2Insert in Section 4.3 Detection Layer, after the threat score equation.Binary or multiclass detection decision rule.Define the threshold rule that maps the threat score to benign, suspicious, or malicious classes. If multiclass attacks are used, define the argmax decision over class probabilities.E3Insert in Section 4.5 Response Layer.Response reward function.Define the reward as a function of correct containment, mitigation speed, intelligence gain, false blocking penalty, resource cost, and service-disruption penalty. This is necessary to justify how the response agent learns safe actions.E4Insert in Section 4.5 Response Layer, after the reward function.Policy update rule for the RL or MARL response engine.Include the Q-learning, deep Q-network, actor-critic, or MARL update equation actually used. The current text mentions reinforcement learning but does not define the learning rule.E5Insert in Section 4.7 Multi-Agent Coordination.Multi-agent alert aggregation or consensus equation.Define how agent-level confidence values are combined into a global threat decision, for example weighted averaging, voting, or consensus update. The weights should reflect agent reliability or recent performance.E6Insert in Section 4.4 Deception Layer or Section 5.3.Deception engagement rate.Define engagement rate as successfully engaged deceptive sessions divided by redirected suspicious sessions, with a clear definition of successful engagement, such as dwell time above a threshold or execution of at least one meaningful interaction.E7Insert in Section 5.2 Evaluation Metrics.Detection metric equations.Define accuracy, precision, recall, F1-score, false positive rate, false negative rate, ROC-AUC if used, and macro-average or weighted-average aggregation for multiclass evaluation.E8Insert in Section 5.4 Autonomous Response Evaluation.Containment time, mitigation time, and mean time to respond.Define how initial containment time, full mitigation time, and MTTR are measured from attack start, alert generation, response initiation, and successful mitigation. This will make Table 10 measurable.E9Insert in the new Robustness Evaluation subsection.Performance degradation under robustness stress.Define percentage degradation as the relative drop from clean-test performance to stressed-test performance. Use this for unseen attacks, concept drift, noise, and delayed communication.E10Insert in Section 5.5 Multi-Agent Coordination Analysis.Scalability and communication overhead.Define throughput, average decision latency, and communication overhead per agent or per event. This will support scalability claims with measurable quantities.                  HoneyBee BehaviourCyber Defense FunctionScout BeesThreat Monitoring AgentsGuard BeesThreat Detection AgentsWaggle Dance CommunicationAgent-to-Agent Intelligence SharingColony DefenseCoordinated Response ActionsCollective LearningThreat Intelligence Repository & Model Updates